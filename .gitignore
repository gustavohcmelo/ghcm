# Runtime — não versionar
state/
logs/
current-project.txt

# Config customizada do usuário (template versionado: config.example.sh)
config.sh

# Editor / OS
*.swp
.DS_Store

# Blueprint local (design notes não versionadas)
docs/design/
